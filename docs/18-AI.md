# AI

## Purpose
The `AiJob` Prisma model, `ai` API module, and `apps/workers`'s AI processors suggest an AI-assisted image preprocessing capability (referenced conceptually in `docs/architecture/architecture.md`'s workflow overview as "assets may pass through AI preprocessing jobs").

## Users / Roles
Not established — no working code exists to determine actual role interaction.

## Current Implementation
**Scaffolding exists but production workflow is not implemented.** `services/api/src/modules/ai/ai.service.ts` contains exactly one function: `getAiStatus()` returning `{ module: 'ai', status: 'placeholder' }`. `ai.routes.ts` exposes only `GET /ai/health`. `apps/workers/src/processors/ai.processor.ts` and `ai-processing.processor.ts` are placeholder objects (`{ name: 'ai-processor', status: 'placeholder' }`), not working job logic. `services/api/src/queues/ai.queue.ts` exists as a producer, but nothing meaningfully consumes it.

## Frontend
No frontend was found consuming an `ai`-module endpoint or displaying AI-processing status.

## Backend
`services/api/src/modules/ai/` — controller/routes/service (placeholder)/types/validator present but unused beyond the health check.

## Database
`AiJob` model exists (`assetId`, `provider`, `status` enum `QUEUED/RUNNING/SUCCEEDED/FAILED/CANCELED`, `inputUrl`, `outputUrl`, `errorMessage`) but is **not written to or read from** by any traced order/asset code path.

## APIs
Only `GET /ai/health`.

## Business Rules
None implemented.

## Workflow
None implemented. No image in the current system passes through any AI processing step — every "editing" step is manual (editor uploads a deliverable directly).

## Permissions
N/A — no functional endpoints beyond health check.

## Validation
N/A.

## Error Handling
N/A.

## Dependencies
N/A currently; would depend on `assets` (input/output) and `apps/workers` (execution) if built.

## Current Status
**STUB.**

## Known Limitations
No AI capability exists anywhere in the current runtime path, despite: an `AiJob` model, an `ai` API module, an `ai.queue.ts` producer, and two AI-named worker processor files. All are non-functional scaffolding.

## Target
Not established in current codebase as a committed, dated plan — per this documentation's own rule, marketing/product claims of "AI processing" should not be made until this module is actually implemented (see `docs/00-PRODUCT-OVERVIEW.md`, which explicitly flags this).

## Gap
Everything: no working AI provider integration, no queue consumer logic, no UI, and the `AiJob` model is unused.
