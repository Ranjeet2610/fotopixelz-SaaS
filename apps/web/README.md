# web

Placeholder scaffold for the future Fotopixelz public marketing website (`fotopixelz.com`). Not yet in active development — see `apps/client` for the authenticated client application.

```bash
pnpm --filter web run dev
```
